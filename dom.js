//manipulando el DOM

// const heading = document.querySelector(".heading");

// heading.textContent = "Manipulando el DOM"; // puedo cambiar el texto 

// console.log(heading.textContent);

// const inputNombre = document.querySelector("#nombre")
// inputNombre.value = "Un valor por default";   // podemos generar un valor por default dentro del input

// const enlace = document.querySelectorAll('.enlaces a')
// enlace.forEach(enlace => enlace.textContent = "Enlaces JS") // ya que es un selector all tenemos que iterar cada enlace para que pueda activarse

///////////////////////////////////////////////////////////////////
//Manipulando mediante clicks
// const heading = document.querySelector(".heading");

// heading.addEventListener('click', () => {
//     heading.textContent = "Lo estamos Manipulando"  // una vez que clickea se cambia el texto
// }
// )
////////////////////////////////////////////////////////////////////

//Manipulando Imputs

// const name = document.querySelector("#nombre")

// name.addEventListener('input', function(e){
//     console.log(e.target.value); // de esta forma podemos ver lo que se escribe dentro de la consola
// })

// const password = document.querySelector('#password')

// password.addEventListener('input', functionpassword)

// function functionpassword (e){
//     password.type = "text"

//     setTimeout(() => {
//         password.type = "password"

//     }, 100);
//     console.log(e.target.value) 
// }  // de esta forma podemos cambiar el forma en la que se escribre el password



/////////////////////////////////////////////////////////////////////////////
